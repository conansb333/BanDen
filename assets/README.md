# Assets

Brand and documentation assets. The application icons are **generated** —
run `node scripts/generate-icons.mjs` after changing the mark; the outputs
land in `apps/desktop/src-tauri/icons/`.
